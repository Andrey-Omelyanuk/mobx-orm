import { reaction, runInAction } from 'mobx'
import { EQ, EQ_Filter } from './EQ'


describe('AND Filter', () => {

    describe('constructor', () => {
        it('field=A', () => {
            // expect(new EQ_Filter('A')).toMatchObject({field: 'A'    , value: undefined })
        })
    })

})